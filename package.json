{
  "name": "electron_startup",
  "version": "1.0.0",
  "description": "えれくとろんことはじめ",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "electron": "electron .",
    "build-for-electron": "parcel build -d dist ./src/index.html --public-url ./"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bulma": "^0.7.5",
    "electron": "^5.0.4",
    "parcel": "^1.12.3"
  },
  "devDependencies": {
    "sass": "^1.22.0"
  }
}
